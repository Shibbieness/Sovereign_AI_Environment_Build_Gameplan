"""
VI Builder — Virtual Intelligence Builder.

A local-first cognitive infrastructure factory: filesystem sources are
wired in (L1), their contents digested into ML processes across the tier
stack (L2/L3), stored and tracked (L4), routed by CALS (L5, not yet built),
and given to Conceptual AIs bounded in Cal's Castles (L6, not yet built).

This package is Phase 1: L1 Filesystem Daemon, L2 Ingestion, L3 ML Process
Factory (Tier 4a/4b), L4 Process Registry, and a CLI. Stdlib-only —
no external dependencies, matching the "zero external dependency" property
the spec assigns to local-path Filesystem Sources.

Deliberately has no import relationship with sovereign_py/. VI Builder
treats other directories (including sovereign_py/) as Filesystem Sources it
observes from the outside — see codex reference: ml-filesystem-monolith
sub-skill E, "VI Builder Bridge".
"""
