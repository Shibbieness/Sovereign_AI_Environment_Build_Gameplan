"""
QRen — the generalization/encoding layer from ml-filesystem-monolith
sub-skill C ("QRen Magic Circle").

Block type taxonomy (14 types), the QRCF wire format, the three
pre-encoded Runic tokens (TB/EA/IF), the Crystal Slime lifecycle, and the
Magic Circle three-mode I/O boundary. Self-contained — no import
dependency on sovereign_py/ or vi_builder/; those systems could adopt QRen
encoding, but QRen doesn't require either to exist.
"""
