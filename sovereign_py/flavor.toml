[flavor]
name = "ml-filesystem"
version = "1.9.0"
license = "AGPL-3.0-or-later"
entrypoint = "vanilla_flavor:run"
capabilities = [
  "status",
  "self-test",
  "stores",
  "fs-write",
  "fs-read",
  "fs-list",
  "fs-search",
  "chains",
  "training-blocks",
]
